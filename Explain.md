# 팀원용 설명 — 지금까지 뭘 만들었나

> 코드를 안 봐도 읽히도록 쓴 문서입니다. 실행 방법과 지표 정의는 [README.md](README.md),
> 코드 규칙은 [CLAUDE.md](CLAUDE.md) 에 있습니다. 여기는 **왜 이렇게 만들었는지**를 씁니다.

* 저장소: https://github.com/KangwonVibeCoding/kangwonland-vip-crosssell
* 배포: https://kangwonland-vip-crosssell-2vubchckkyhe8ehii6hbiu.streamlit.app
* 현재: **4개 탭 전부 동작 · 테스트 62개 통과 · Cloud 배포 완료**

---

## 1. 이 대시보드가 답하려는 질문

강원랜드는 카지노 유입 데이터와 리조트 매출 데이터를 둘 다 공개합니다. 그런데 서로
다른 파일로 분리돼 있어서 **"언제 · 누구에게 · 무엇을 제안할지"** 를 한 화면에서
판단할 수가 없습니다.

우리가 검증한 가설은 하나입니다.

> 카지노로 들어온 고객의 소비력은 리조트 내 고마진 채널로 이어진다.
> 그렇다면 유입이 큰 날을 미리 알고 그날(또는 그 다음 날) 고마진 상품을 제안하면
> 교차판매가 늘어난다.

대시보드는 이 문장을 **유입 강도 → VIP 가용 모수 → 채널별 반응 시차 → 대상 상품**
이라는 하나의 체인으로 계산해서, 마지막에 "이 날짜에 이 상품을 이 채널로" 라는
실행 목록을 냅니다.

---

## 2. 실데이터로 확인한 것 — 이게 우리 발표의 핵심입니다

전부 **가정이 아니라 원본 CSV 를 직접 계산한 값**입니다. 숫자는 테스트에 기대값으로
박아뒀기 때문에 코드를 고치다 흔들리면 바로 잡힙니다.

### ① 가설이 맞았습니다 (2024-12, 31일 실제 날짜 조인)

| 유입 지표 ↔ 판매 | Pearson | Spearman |
|---|---|---|
| 내국인 총 접수자 ↔ 카지노 식음 | **0.801** | 0.859 |
| 내국인 총 접수자 ↔ 룸서비스 | **0.740** | 0.827 |
| 내국인 총 접수자 ↔ 지역특산품 | 0.380 | 0.511 |

카지노 유입과 리조트 F&B 소비가 **r = 0.80** 으로 같이 움직입니다.

**그런데 이걸 그대로 발표하면 안 됩니다.** 토요일엔 유입도 많고 매출도 많으니
**요일이 양쪽을 동시에 밀어올리는 공통 원인**일 수 있습니다. 그래서 요일을 통제한
편상관을 계산했습니다.

| 채널 | 원 상관 | **요일 통제 후** | p | 판정 |
|---|---|---|---|---|
| 카지노 식음 | 0.801 | **0.370** | 0.044 | 간신히 유의 |
| 룸서비스 | 0.740 | **0.558** | 0.0015 | **견고** |
| 지역특산품 | 0.380 | 0.176 | 0.357 | 유의하지 않음 |

**절반 이상이 주말 효과였습니다.** 그리고 순서가 뒤집힙니다 — 원 상관은 카지노 식음이
1위지만, 요일을 빼면 **룸서비스가 유일하게 견고하게 남습니다.**

> 이게 우리 발표에서 가장 강한 카드라고 봅니다. **순진한 r=0.80 보다, 스스로 검증해서
> 깎아낸 0.37 이 신뢰를 얻습니다.** "주말 효과 아니냐" 는 질문이 나오면 이미 답해놓은
> 상태가 되고요. 요일은 마케팅으로 바꿀 수 없는 변수이므로, **통제 후에 남는 관계가
> 실제 개입 여지**입니다 → 교차판매 1순위는 룸서비스입니다.

재현: `python scripts/diagnose_confound.py`

### ② 최대 발견 — 채널마다 반응하는 날이 다릅니다

유입(D일) 과 판매(D+k일) 의 상관을 k 를 바꿔가며 계산했습니다.

| 채널 | D+0 | D+1 | D+2 | 해석 |
|---|---|---|---|---|
| 카지노 식음 | **+0.80** | +0.30 | −0.30 | 당일 즉시 소비 |
| 룸서비스 | **+0.73** | +0.27 | −0.35 | 당일(숙박) 소비 |
| 지역특산품 | +0.37 | **+0.47** | +0.10 | **익일 = 체크아웃 선물** |

**특산품은 유입 당일이 아니라 그 다음 날 팔립니다.**

⚠️ **단, 이 표만으로 D+1 을 주장하면 반박당합니다.** `0.467` 과 `0.370` 의 차이를
부트스트랩으로 검정하면 95% CI 가 `[−0.492, +0.511]` 로 **0을 포함**합니다. n≈30 에서
두 상관계수의 차이는 사실상 동전 던지기입니다.

**진짜 근거는 요일 프로파일입니다.**

| 계열 | 토 → 일 | 해석 |
|---|---|---|
| ARS 유입 | 1.45 → 1.31 | 토요일이 피크 |
| 카지노 식음 | 1.27 → 1.45 | 주말 내내 높음 |
| 룸서비스 | 1.32 → 1.39 | 주말 내내 높음 |
| **지역특산품** | **0.92 → 1.63 (+77%)** | **일요일에만 몰림** |

다른 채널은 토·일 둘 다 높은데 **특산품만 토요일에 평균 이하로 죽어 있다가 일요일에
튑니다.** 유입 피크는 토요일이고요. 이건 상관계수와 달리 눈으로 바로 보이고 표본
크기에 덜 민감합니다.

> **발표할 때**: 래그 상관 표는 "방향을 시사한다" 정도로만 말하고, 주장의 무게는
> 요일 표에 실으세요. **서로 독립적인 두 관측이 같은 방향을 가리킨다** 는 구조로
> 설명하면 단단합니다.
>
> **실행 함의**: 유입 피크일 *당일* 엔 룸서비스·F&B, *익일 오전* 엔 특산품 쿠폰.
> 이게 탭3 의 내용이고, 다른 팀과 갈리는 지점이라고 봅니다.

### ③ 계절성

| | 값 (2023~2024) | 2023 | 2024 | 쓰임 |
|---|---|---|---|---|
| 특산품 9월 | 1.43 | 1.37 | 1.48 | 명절 선물 시즌 — 탭4 |
| 룸서비스 1월 | 1.21 | 1.20 | 1.23 | 연초 숙박 수요 |

> 판매 데이터가 2년치로 늘면서 2년 평균으로 조금 희석됐습니다(9월 1.48→1.43).
> 대신 **같은 피크가 두 해에서 독립적으로 재현**됩니다 — 1년치일 때는 "그 해에
> 마침 이벤트가 있었다"와 구분이 안 됐는데, 이제 계절성이라고 말할 수 있습니다.
> 발표에서 이 점을 짚으면 좋습니다.

---

## 3. 가장 큰 설계 결정 3가지

### (1) 시간축을 3구간으로 쪼갰습니다

ARS(유입) 데이터와 판매 데이터의 **기간이 부분적으로만 겹칩니다.** 그냥 섞으면
"없는 근거로 그린 차트" 가 되기 때문에, 구간을 나누고 각 구간에서 할 수 있는 분석만
합니다.

| 구간 | 기간 | 있는 데이터 | 할 수 있는 것 | 화면 배지 |
|---|---|---|---|---|
| **A** | 2024-12 (31일) | ARS + 판매 | 진짜 날짜 조인 — 상관·래그 실측 | `실측 조인` |
| **B** | 2023~2024 (731일) | 판매만 | CAI(유입 프록시) + 요일 브릿지 | `요일 브릿지` |
| **C** | 2026 상반기 (181일) | ARS만 | 최신 유입 트렌드 | `최신 유입` |

**모든 차트에 이 배지를 답니다.** 지금 보는 숫자가 실증인지 추정인지 화면에 항상
드러나게 한 것이 이 프로젝트의 신뢰 장치입니다. 심사에서 "그 숫자 근거가 뭐냐" 는
질문이 나오면 배지를 가리키면 됩니다.

> 로더가 `data/raw/ars_*.csv` 를 glob 으로 읽습니다. **ARS 파일을 더 넣기만 하면
> 코드 수정 없이 구간 A 가 자동으로 넓어집니다.** 2024년 나머지 달 ARS 를 구하면
> 전 구간이 실측 조인으로 승격됩니다 — 데이터 더 받으실 때 이게 1순위입니다.

> ### ⚠️ 구간 A 는 파일 하나에 매달려 있습니다 (가장 조심할 것)
>
> `data/raw/ars_20241201_20241231.csv` 31행이 **구간 A 의 유일한 근거**입니다.
> 2026년 ARS 파일에는 2024-12 이 없고 판매 데이터는 2023~2024 라서, 이 파일이
> 빠지면 **ARS 와 판매가 겹치는 날이 0일**이 됩니다. 그러면 r=0.80 배너도,
> 특산품 D+1 도, VIP 상관도 전부 화면에서 사라집니다.
>
> `data/raw/` 는 저장소에 안 올라가므로 이 파일의 유일한 커밋본은
> `data/sample/ars.parquet` 안에 있습니다. 사라졌으면 이렇게 되살립니다:
>
> ```powershell
> & ".\.venv\Scripts\python.exe" scripts\restore_ars_2024.py
> Remove-Item data\processed\*.parquet -ErrorAction SilentlyContinue
> ```
>
> `make_sample.py` 에도 안전장치를 넣었습니다 — 2024-12 이 빠진 ARS 로는
> 축약본을 덮어쓰지 않습니다(덮어쓰면 복원 원천까지 없어지니까요).

### (2) 데이터 로딩을 6계층 폴백으로 만들었습니다

심사 중에 앱이 흰 화면이 되는 게 최악이라, 로더는 **예외를 밖으로 던지지 않습니다.**

```
L1 data/processed/*.parquet   캐시
L2 data/raw/*.csv             원본 (ingest 산출물)
L3 data/sample/*.parquet      배포용 축약본  ← Cloud 는 여기를 씁니다
L4 Open API (data.go.kr)
L5 data/mock/*.csv
L6 코드 내장 상수             ← 파일·네트워크 접근 0 → 절대 실패 없음
```

L6 은 난수를 안 쓰고 **실측 통계를 모사**합니다(요일지수 토 1.45, 특산품 9월 1.48,
D+1 시차). 폴백 상태에서도 차트가 의미 있게 보여야 데모가 성립하기 때문입니다.

탭도 개별로 예외를 격리해서, **한 탭이 죽어도 나머지 3개는 뜹니다.**
뒤집어 말하면 에러가 조용히 한 탭만 죽이므로, 확인할 땐 4개 탭을 다 눌러봐야 합니다.

### (3) `.gitignore` 와 배포의 충돌을 축약본으로 풀었습니다

원본 CSV 는 저장소에 안 올립니다. 그런데 그대로 두면 **Cloud 배포판이 항상 모킹으로
뜹니다** — 심사자가 보는 화면에서 r=0.80 이 실측이 아니게 됩니다.

그래서 CSV 37MB 를 parquet 으로 **1.1MB(약 34배 압축)** 로 줄여 `data/sample/` 에
커밋했습니다. **기간을 자르지 않고 2년 284,866행 전부** 담았습니다 — 몇 달치로 자르는
안도 있었지만 그러면 월 인덱스(9월 1.43 등) 를 배포판이 재현 못 합니다.

`tests/test_deploy_sample.py` 가 **`data/raw` 가 없는 상태를 흉내 내서** 축약본만으로
r=0.801 과 D+1 이 재현되는지 검사합니다. 배포판이 조용히 데모 데이터로 떨어지는 사고를
막는 장치입니다.

---

## 4. 원본 데이터에서 걸린 문제들과 대응

계획을 실제로 바꾼 것들입니다. 코드 곳곳의 방어 로직이 전부 여기서 나왔습니다.

| 문제 | 대응 |
|---|---|
| **주문 시각(hour) 컬럼이 없다** | 원 기획의 "심야 피크타임 분석" 불가 → **요일 × 월 히트맵**으로 대체 |
| **단가 컬럼이 없다** | 고마진은 프록시. 상품명 2티어(프리미엄/선물번들) + 판매 희소도. 단일 정규식이면 특산품 히트율이 31.4% 로 과대해져서 티어를 나눔 |
| **무상 제공 상품이 전체 수량의 약 6%** | `(V)` 접두 30,560개 + **`무료` 포함 414,912개**(2023~2024 합산). `무료` 쪽이 13배 큼. `(V)산삼배양근진` 같은 게 프리미엄 패턴에 걸려서 → 컴프는 티어 강등 + 마진 0 + 점수 0 |
| **lift 가 2,700만까지 발산** | 저유입일 판매가 0인 상품이 72종. 가법 스무딩 + 표본 요건(판매일수 5일/수량 10개) 미달은 `NaN` 으로 둠. **1.0 으로 채우면 없는 근거를 만드는 것** |
| **특산품 2024-12-17 하루가 통째로 없음** | 2년 730일 중 유일한 결측일(다른 두 채널은 결측 0). 일별 최소 판매량이 206 이라 "판매 0인 날" 이 아니라 데이터 누락 → 0 으로 채우지 않고 **조인에서 제외**. 채우면 상관 0.380→0.421 로 왜곡됨 |
| **`총 당첨자` 가 사실상 상수** | 일일 정원 캡(최대 2,999)에 묶여 CV 0.18 → **유입 지표에서 제외**하고 `내국인 총 접수자`(CV 0.37) 사용 |
| **2026년 ARS 에 `내국인 총 접수자` 컬럼이 없음** | 전처리 과정에서 탈락(2026-05 는 옛 파일과 값이 정확히 일치하는데 컬럼만 적음). 그 구간만 유입 지수를 2축(구매 건수·구매율)으로 폴백. **0 으로 채우면 "그 날 접수자 0명"이 지수의 35% 를 차지**하므로 결측은 결측으로 둠 |
| **골프 원본 끝에 빈 행 47개** | 날짜까지 비어 있음 → 파생 컬럼 만들기 *전에* 제거. 뒤에서 지우면 `isocalendar().week` 가 NA 를 int 로 못 바꿔서 예외남 (판매 3종엔 결측 날짜가 없어 여태 안 드러난 버그) |
| **매월 1일 판매량이 1.45배로 튐** | 집계 아티팩트 의심 → 정규화를 5~95 분위 클리핑으로. 단순 min-max 면 그 하루가 스케일을 독점 |
| **컬럼명에 오타·불규칙 공백** (`ARS 담청자`) | 공백·괄호 제거한 정규화 키로 조회 |
| **원본 파일명 규칙이 제각각** | 파일명이 아니라 **헤더 시그니처**로 데이터셋 판정 |

---

## 5. 코드 구조 — 건드릴 때 지킬 경계

```
app.py            오케스트레이션만 (로드 → 필터 → 탭 렌더). 로직 금지
src/views/        화면 4개. render(ctx) 시그니처 하나
src/ui/           streamlit / plotly / pydeck 를 아는 유일한 층
src/analysis/     DataFrame in / DataFrame out 순수 함수. streamlit import 금지
src/data/         pandas 만. streamlit import 금지
config/settings.py  단일 설정 소스 (경로·패턴·가중치·구간 정의)
```

**`src/data` 와 `src/analysis` 가 streamlit 을 import 하지 않는 것이 테스트를 가능하게
하는 전제입니다.** 여기에 `import streamlit` 을 넣으면 62개 테스트가 전부 깨집니다.

가중치·정규식·임계값을 바꾸고 싶으면 **`config/settings.py` 한 곳만** 보면 됩니다.
코드 안에 하드코딩된 숫자는 없습니다.

### 4개 탭

| 탭 | 내용 |
|---|---|
| 📊 메인 | 가설 검증 배너(r=0.80) · KPI · 유입 추이 · VIP 모수 · **VTS 상위 10일 = 캠페인 집행 캘린더** |
| 🍽️ F&B 매칭 | 요일×월 히트맵 · VIP 메뉴 Top15 · 영업장 트리맵 · CSM 테이블 · 번들 추천 |
| ⏱️ 시차 엔진 | **래그 상관 히트맵** · 요일 인덱스 4계열 · D+0/D+1 공략 상품 · 실행 처방 카드 |
| 🎁 로컬 큐레이션 | 특산품 Top20 · 가맹점 지도 · 9월 시즌 분석 · 패키지 제안 |

---

## 6. 실행 방법

```powershell
git clone https://github.com/KangwonVibeCoding/kangwonland-vip-crosssell.git
cd kangwonland-vip-crosssell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
streamlit run app.py
```

**원본 CSV 없이 바로 실행됩니다.** `data/sample/` 축약본이 커밋돼 있어서 clone 직후
실데이터 화면이 그대로 뜹니다. API 키도 필요 없습니다.

```powershell
pytest tests\ -q          # 62개
```

한글이 깨지면 앞에 `$env:PYTHONIOENCODING="utf-8"` 을 붙이세요. 콘솔 문제일 뿐
데이터는 멀쩡합니다.

---

## 7. 지금 상태와 남은 일

### 되는 것
- 4개 탭 전부 렌더 · 테스트 62개 통과 · Cloud 배포 · 로그인 없이 접근 가능
- ARS + 판매 3종은 **실데이터**로 동작

### 아직 안 된 것
- **성별·연령 / 가맹점** 2종은 Open API 라 인증키가 필요합니다. 지금은 내장 폴백으로
  뜹니다 (화면 상단 노란 배너에 이 둘의 이름이 찍힙니다). 앱은 정상 동작하고,
  키를 넣으면 탭1 인구통계와 탭4 지도가 실데이터로 바뀝니다.
- 스크린샷, 다크모드 확인, 3분 시연 리허설

### 팀원이 맡아주면 좋은 것 (우선순위 순)

1. **2024년 나머지 달의 ARS 데이터 확보** — 가장 임팩트가 큽니다. 파일만 넣으면
   구간 A(실측 조인)가 자동으로 넓어져서 31일 표본이라는 최대 약점이 사라집니다.
2. **data.go.kr 인증키 + 활용신청 승인** — `고객성별연령분석현황`, `하이원포인트
   가맹점 상세정보` 두 건. ⚠️ 키는 **Decoding 쪽**을 써야 합니다 (코드가 URL 인코딩을
   자동으로 해서, Encoding 키를 넣으면 이중 인코딩으로 인증 실패).
3. **발표 스토리 정리** — 3분. 권장 순서:
   **① r=0.80 → ② 요일 통제하니 0.37 (절반은 주말 효과였다) → ③ 그래도 룸서비스는
   0.56 으로 견고 = 1순위 채널 → ④ 특산품은 토 0.92 → 일 1.63 = D+1 체크아웃 →
   ⑤ 가맹점 큐레이션.**
   ②를 우리가 먼저 꺼내는 게 핵심입니다. 감추면 질문으로 돌아옵니다.
4. **스크린샷 4장** (`docs/screenshots/`)

### 주의사항 몇 개

- **키를 코드나 파일에 커밋하지 마세요.** 로컬은 `.streamlit/secrets.toml`(gitignore),
  Cloud 는 앱 Settings → Secrets 에 넣습니다. 저장소가 Public 입니다.
- **스키마나 전처리를 바꿨으면 `data/processed/*.parquet` 을 지우세요.** 로더가 캐시를
  먼저 읽어서 옛 결과가 계속 나옵니다.
- **`tests/test_stats.py` 의 상수는 실측 회귀 기준값입니다.** 테스트가 깨졌다고 숫자를
  고치지 말고, 왜 바뀌었는지 먼저 확인하세요. 대부분 전처리 버그입니다.
- pandas 가 **3.0.5** 입니다(2.x 아님). 연쇄 대입 대신 `assign`/`loc` 을 쓰세요.
