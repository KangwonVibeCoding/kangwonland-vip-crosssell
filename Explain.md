# 팀원용 설명 — 지금까지 뭘 만들었나

> 코드를 안 봐도 읽히도록 쓴 문서입니다. 실행 방법과 지표 정의는 [README.md](README.md),
> 코드 규칙은 [CLAUDE.md](CLAUDE.md) 에 있습니다. 여기는 **왜 이렇게 만들었는지**를 씁니다.

* 저장소: https://github.com/KangwonVibeCoding/kangwonland-vip-crosssell
* 배포: https://kangwonland-vip-crosssell-2vubchckkyhe8ehii6hbiu.streamlit.app
* 현재: **4개 탭 전부 동작 · 테스트 151개 통과 · Cloud 배포 완료**

---

## 1. 이 대시보드가 답하려는 질문

강원랜드는 카지노 유입 데이터와 리조트 매출 데이터를 둘 다 공개합니다. 그런데 서로
다른 파일로 분리돼 있어서 **"언제 · 누구에게 · 무엇을 제안할지"** 를 한 화면에서
판단할 수가 없습니다.

우리가 검증한 가설은 하나입니다.

> 카지노로 들어온 고객의 소비력은 리조트 내 고마진 채널로 이어진다.
> 그렇다면 유입이 큰 날을 미리 알고 그날(또는 그 다음 날) 고마진 상품을 제안하면
> 교차판매가 늘어난다.

대시보드는 이 문장을 **유입 강도 → VIP 가용 모수 → 채널별 반응 시차 → 대상 상품**
이라는 하나의 체인으로 계산해서, 마지막에 "이 날짜에 이 상품을 이 채널로" 라는
실행 목록을 냅니다.

---

## 2. 실데이터로 확인한 것 — 이게 우리 발표의 핵심입니다

전부 **가정이 아니라 원본 CSV 를 직접 계산한 값**입니다. 숫자는 테스트에 기대값으로
박아뒀기 때문에 코드를 고치다 흔들리면 바로 잡힙니다.

### ① 가설이 맞았습니다 (2023~2024, **731일 실제 날짜 조인**)

> 2026-08-11 에 ARS 전처리 통합본(2023-01~2026-06)이 들어왔습니다. 그전까지
> 유입과 판매가 겹치는 날은 2024-12 의 **31일뿐**이었는데, 이제 판매 전 기간
> **731일**이 조인됩니다. 표본이 24배가 됐고 **결론 하나가 뒤집혔습니다**(②).

| 유입 지표 ↔ 판매 | 31일 창 | **731일 창** |
|---|---|---|
| 내국인 총 접수자 ↔ 카지노 식음 | 0.801 | **0.801** |
| 내국인 총 접수자 ↔ 룸서비스 | 0.740 | **0.765** |
| 내국인 총 접수자 ↔ 지역특산품 | 0.380 | **0.409** |

**r = 0.80 이 표본 24배에서 소수점 셋째 자리까지 그대로입니다.** 나머지 둘은 오히려
올라갔습니다. 이 항목은 강화됐습니다.

**그래도 이걸 그대로 발표하면 안 됩니다.** 토요일엔 유입도 많고 매출도 많으니
**요일이 양쪽을 동시에 밀어올리는 공통 원인**일 수 있습니다. 요일을 통제한 편상관:

| 채널 | 원 상관 | **요일 통제 후** | p | 판정 |
|---|---|---|---|---|
| 카지노 식음 | 0.801 | **0.617** | ≤0.001 | **견고** |
| 룸서비스 | 0.765 | **0.596** | ≤0.001 | **견고** |
| 지역특산품 | 0.409 | **0.214** | ≤0.001 | **견고** |

**요일이 설명하는 몫은 약 23% 입니다.** 세 채널 전부 p ≤ 0.001 로 견고합니다.

> 31일 창에서는 그림이 달랐습니다 — 카지노 0.370(p 0.043, 간신히), 룸서비스
> 0.558, 특산품 0.176(p 0.344, 유의하지 않음). "절반 이상이 주말 효과였다",
> "룸서비스가 1순위" 라는 서사가 여기서 나왔는데, **둘 다 31일 표본의 산물**이었습니다.
> 731일에서는 카지노가 1위를 되찾고 특산품도 유의해집니다.

> **발표할 때**: "원 상관 0.80 → 요일 통제 0.62, 그래도 세 채널 다 p<0.001" 이
> 지금의 카드입니다. 스스로 통제해서 깎아낸 값을 먼저 꺼내는 구조는 그대로 유효하고,
> 표본이 731일이라 "주말 효과 아니냐"에 훨씬 단단하게 답할 수 있습니다.

재현: `python scripts/diagnose_confound.py`

### ② 철회합니다 — "특산품 D+1" 은 31일 표본의 산물이었습니다

한동안 이걸 최대 발견으로 적어뒀습니다. **731일 데이터가 들어오자 사라졌습니다.**
정직하게 남겨둡니다.

| 특산품 래그 | D+0 | D+1 | best |
|---|---|---|---|
| 구간 A (31일) | 0.370 | **0.467** | D+1 ← 옛 결론 |
| 2023년 (365일) | **0.490** | 0.299 | D+0 |
| 2024년 (366일) | **0.422** | 0.261 | D+0 |
| **전체 (731일)** | **0.313** | 0.163 | **D+0** |

**두 해가 각각 독립적으로 D+0 을 가리키고 격차도 큽니다.** D+1 은 31일 창에서만
나옵니다. 사실 예고는 있었습니다 — `0.467` 과 `0.370` 의 차이를 부트스트랩하면
95% CI 가 `[−0.492, +0.511]` 로 **0을 포함**했습니다. n≈30 에서 두 상관계수의 차이는
동전 던지기라고 이미 적어뒀었고, 실제로 그랬던 겁니다.

**그런데 살아남은 게 있습니다 — 피크 요일이 하루 어긋납니다.**

| 계열 (731일) | 토 | 일 | 피크 |
|---|---|---|---|
| ARS 유입 | **1.38** | 1.25 | 토요일 |
| 지역특산품 | 1.20 | **1.36** | **일요일** |

731일에서도 유입은 토요일에, 특산품은 일요일에 정점입니다. 주말 내내 둘 다 높아서
*당일 상관*이 더 강할 뿐, **피크가 어긋난다는 관찰 자체는 참**입니다.

> **발표할 때**: "체크아웃 선물" 서사는 **피크 요일 차이**로 말하고, 래그 상관으로는
> 말하지 마세요. "유입은 토요일이 정점인데 특산품은 일요일이 정점이다" 까지가
> 데이터가 지지하는 문장입니다. *"그래서 D+1 에 팔린다"* 는 한 걸음 더 나간 주장이고,
> 731일로 재보니 지지되지 않았습니다.
>
> 질문이 나오면 이렇게 답하시면 됩니다 — **"처음엔 31일치로 D+1 이라고 봤는데,
> 2년치를 확보해 다시 재보니 당일이 더 강했습니다. 남은 건 피크 요일 차이입니다."**
> 스스로 뒤집은 결과를 보여주는 게 오히려 신뢰를 얻는 자리라고 봅니다.
>
> **실행 함의**: 유입 피크일 당일에 F&B·룸서비스를 밀고, 특산품은 **일요일**에
> 붙입니다. "익일 오전 쿠폰" 이 아니라 "일요일 체크아웃 동선" 으로 바뀝니다.

### ③ 계절성

| | 값 (2023~2024) | 2023 | 2024 | 쓰임 |
|---|---|---|---|---|
| 특산품 9월 | 1.43 | 1.37 | 1.48 | 명절 선물 시즌 — 탭4 |
| 룸서비스 1월 | 1.21 | 1.20 | 1.23 | 연초 숙박 수요 |

> 판매 데이터가 2년치로 늘면서 2년 평균으로 조금 희석됐습니다(9월 1.48→1.43).
> 대신 **같은 피크가 두 해에서 독립적으로 재현**됩니다 — 1년치일 때는 "그 해에
> 마침 이벤트가 있었다"와 구분이 안 됐는데, 이제 계절성이라고 말할 수 있습니다.
> 발표에서 이 점을 짚으면 좋습니다.

---

## 3. 가장 큰 설계 결정 3가지

### (1) 시간축을 3구간으로 쪼갰습니다

ARS(유입) 데이터와 판매 데이터의 **기간이 부분적으로만 겹칩니다.** 그냥 섞으면
"없는 근거로 그린 차트" 가 되기 때문에, 구간을 나누고 각 구간에서 할 수 있는 분석만
합니다.

| 구간 | 기간 | 있는 데이터 | 할 수 있는 것 | 화면 배지 |
|---|---|---|---|---|
| **A** | 2024-12 (31일) | ARS + 판매 | 좁은 창 대조용 — 최초 분석의 원점 | `31일 창` |
| **B** | 2023~2024 (**731일**) | ARS + 판매 | **실측 조인 — 지금의 기본 창** | `실측 조인` |
| **C** | 2025~2026 상반기 (516일) | ARS만 | 최신 유입 트렌드 | `최신 유입` |

**2026-08-11 에 구간 B 가 프록시에서 실측 조인으로 승격됐습니다.** 예전엔 판매만
있어서 CAI(유입 프록시)와 요일 브릿지로 때웠는데, 이제 진짜 ARS 가 731일 전부
붙습니다. 앱 기본 창도 A → B 로 옮겼습니다.

**구간 A 는 지우지 않았습니다.** 같은 코드가 창에 따라 다른 답을 내는 사례(②의
특산품 래그)를 대조하려면 두 창이 다 필요합니다. 화면에서 구간을 A 로 바꾸면
옛 결론이 그대로 재현되는 것을 볼 수 있습니다 — 이것도 하나의 근거입니다.

**모든 차트에 이 배지를 답니다.** 지금 보는 숫자가 어느 창의 값인지 화면에 항상
드러나게 한 것이 이 프로젝트의 신뢰 장치입니다. 심사에서 "그 숫자 근거가 뭐냐" 는
질문이 나오면 배지를 가리키면 됩니다.

> ### ARS 파일 2개를 함께 두어야 합니다
>
> | 파일 | 담는 것 |
> |---|---|
> | `ars_20230101_20260630.csv` | 2023-01~2026-06 **1,247일** (2025-11 은 이상치라 제외) |
> | `ars_20241201_20241231.csv` | 2024-12 31일 — **ARS·모바일 접수자 분해**를 보완 |
>
> 통합본에는 당첨자 분해만 있고 접수자 분해가 없어서 옛 파일이 그 31일을 채웁니다.
> 로더가 날짜로 중복을 제거하므로 둘을 같이 둬도 안전합니다.
>
> `data/raw/` 는 저장소에 안 올라가므로 커밋본은 `data/sample/ars.parquet` 안에
> 있습니다. 2024-12 원본이 사라졌으면 이렇게 되살립니다:
>
> ```powershell
> & ".\.venv\Scripts\python.exe" scripts\restore_ars_2024.py
> Remove-Item data\processed\*.parquet -ErrorAction SilentlyContinue
> ```

### (2) 데이터 로딩을 6계층 폴백으로 만들었습니다

심사 중에 앱이 흰 화면이 되는 게 최악이라, 로더는 **예외를 밖으로 던지지 않습니다.**

```
L1 data/processed/*.parquet   캐시
L2 data/raw/*.csv             원본 (ingest 산출물)
L3 data/sample/*.parquet      배포용 축약본  ← Cloud 는 여기를 씁니다
L4 Open API (data.go.kr)
L5 data/mock/*.csv
L6 코드 내장 상수             ← 파일·네트워크 접근 0 → 절대 실패 없음
```

L6 은 난수를 안 쓰고 **구간 A(2024-12) 실측을 모사**합니다(ARS 요일지수 토 1.45, 특산품 9월 1.43,
D+1 시차 — 구간 A 한정 값입니다). 폴백 상태에서도 차트가 의미 있게 보여야 데모가 성립하기 때문입니다.
`tests/test_fallback.py` 가 이 모사값을 실측 회귀 기준값과 대조해 고정합니다 —
2년 확장 때 여기 갱신을 빠뜨려 한동안 1년치 구값을 모사하고 있었습니다.

탭도 개별로 예외를 격리해서, **한 탭이 죽어도 나머지 3개는 뜹니다.**
뒤집어 말하면 에러가 조용히 한 탭만 죽이므로, 확인할 땐 4개 탭을 다 눌러봐야 합니다.

### (3) `.gitignore` 와 배포의 충돌을 축약본으로 풀었습니다

원본 CSV 는 저장소에 안 올립니다. 그런데 그대로 두면 **Cloud 배포판이 항상 모킹으로
뜹니다** — 심사자가 보는 화면에서 r=0.80 이 실측이 아니게 됩니다.

그래서 CSV 37MB 를 parquet 으로 **1.1MB(약 34배 압축)** 로 줄여 `data/sample/` 에
커밋했습니다. **기간을 자르지 않고 2년 284,866행 전부** 담았습니다 — 몇 달치로 자르는
안도 있었지만 그러면 월 인덱스(9월 1.43 등) 를 배포판이 재현 못 합니다.

`tests/test_deploy_sample.py` 가 **`data/raw` 가 없는 상태를 흉내 내서** 축약본만으로
r=0.801 과 구간 A 의 D+1 이 재현되는지 검사합니다. 배포판이 조용히 데모 데이터로 떨어지는 사고를
막는 장치입니다.

---

## 4. 원본 데이터에서 걸린 문제들과 대응

계획을 실제로 바꾼 것들입니다. 코드 곳곳의 방어 로직이 전부 여기서 나왔습니다.

| 문제 | 대응 |
|---|---|
| **주문 시각(hour) 컬럼이 없다** | 원 기획의 "심야 피크타임 분석" 불가 → **요일 × 월 히트맵**으로 대체 |
| **단가 컬럼이 없다** | 고마진은 프록시. 상품명 2티어(프리미엄/선물번들) + 판매 희소도. 단일 정규식이면 특산품 히트율이 31.4% 로 과대해져서 티어를 나눔 |
| **무상 제공 상품이 전체 수량의 약 6%** | `(V)` 접두 30,560개 + **`무료` 포함 414,912개**(2023~2024 합산). `무료` 쪽이 13배 큼. `(V)산삼배양근진` 같은 게 프리미엄 패턴에 걸려서 → 컴프는 티어 강등 + 마진 0 + 점수 0 |
| **lift 가 2,700만까지 발산** | 저유입일 판매가 0인 상품이 72종. 가법 스무딩 + 표본 요건(판매일수 5일/수량 10개) 미달은 `NaN` 으로 둠. **1.0 으로 채우면 없는 근거를 만드는 것** |
| **특산품 2024-12-17 하루가 통째로 없음** | 2년 730일 중 유일한 결측일(다른 두 채널은 결측 0). 일별 최소 판매량이 206 이라 "판매 0인 날" 이 아니라 데이터 누락 → 0 으로 채우지 않고 **조인에서 제외**. 채우면 상관 0.380→0.421 로 왜곡됨 |
| **`총 당첨자` 가 사실상 상수** | 일일 정원 캡(최대 2,999)에 묶여 CV 0.18 → **유입 지표에서 제외**하고 `내국인 총 접수자`(CV 0.37) 사용 |
| **~~2026년 ARS 에 `내국인 총 접수자` 컬럼이 없음~~** ✅ 해소 | 2026-08-11 통합본이 총접수자를 전 구간에 채워 폴백이 발동하지 않습니다. **다만 폴백 코드는 남겨뒀습니다** — 총접수자 없는 파일이 또 들어오면 "그 날 접수자 0명"이 지수의 35% 를 차지하는 상태로 조용히 돌아가기 때문입니다. 실데이터 대신 합성 프레임으로 그 경로를 계속 테스트합니다 |
| **긴 창에서 lift 가 상품 수명주기를 잼** | 유입이 2023→2024 에 21.6% 감소해서, 전역 분위로 고·저 유입일을 나누면 고유입일이 2023 에 몰림 → 2023 상반기 단종 상품이 lift 47.05 로 1위. **달 안에서 고·저를 나눠**(`hi_lo_days_by_month`) 항상 동시대끼리 비교하게 고침 → 최댓값 2.02 |
| **골프 원본 끝에 빈 행 47개** | 날짜까지 비어 있음 → 파생 컬럼 만들기 *전에* 제거. 뒤에서 지우면 `isocalendar().week` 가 NA 를 int 로 못 바꿔서 예외남 (판매 3종엔 결측 날짜가 없어 여태 안 드러난 버그) |
| **매월 1일 판매량이 1.45배로 튐** | 집계 아티팩트 의심 → 정규화를 5~95 분위 클리핑으로. 단순 min-max 면 그 하루가 스케일을 독점 |
| **컬럼명에 오타·불규칙 공백** (`ARS 담청자`) | 공백·괄호 제거한 정규화 키로 조회 |
| **원본 파일명 규칙이 제각각** | 파일명이 아니라 **헤더 시그니처**로 데이터셋 판정 |

---

## 5. 코드 구조 — 건드릴 때 지킬 경계

```
app.py            오케스트레이션만 (로드 → 필터 → 탭 렌더). 로직 금지
src/views/        화면 4개. render(ctx) 시그니처 하나
src/ui/           streamlit / plotly / pydeck 를 아는 유일한 층
src/analysis/     DataFrame in / DataFrame out 순수 함수. streamlit import 금지
src/data/         pandas 만. streamlit import 금지
config/settings.py  단일 설정 소스 (경로·패턴·가중치·구간 정의)
```

**`src/data` 와 `src/analysis` 가 streamlit 을 import 하지 않는 것이 테스트를 가능하게
하는 전제입니다.** 여기에 `import streamlit` 을 넣으면 151개 테스트가 전부 깨집니다.

가중치·정규식·임계값을 바꾸고 싶으면 **`config/settings.py` 한 곳만** 보면 됩니다.
코드 안에 하드코딩된 숫자는 없습니다.

### 4개 탭

| 탭 | 내용 |
|---|---|
| 📊 메인 | 가설 검증 배너(r=0.80) · KPI · 유입 추이 · VIP 모수 · **VTS 상위 10일 = 캠페인 집행 캘린더** |
| 🍽️ F&B 매칭 | 요일×월 히트맵 · VIP 메뉴 Top15 · 영업장 트리맵 · CSM 테이블 · 번들 추천 |
| ⏱️ 시차 엔진 | **래그 상관 히트맵** · 요일 인덱스 4계열 · D+0/D+1 공략 상품 · 실행 처방 카드 |
| 🎁 로컬 큐레이션 | 특산품 Top20 · 가맹점 지도 · 9월 시즌 분석 · 패키지 제안 |

---

## 6. 실행 방법

```powershell
git clone https://github.com/KangwonVibeCoding/kangwonland-vip-crosssell.git
cd kangwonland-vip-crosssell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
streamlit run app.py
```

**원본 CSV 없이 바로 실행됩니다.** `data/sample/` 축약본이 커밋돼 있어서 clone 직후
실데이터 화면이 그대로 뜹니다. API 키도 필요 없습니다.

```powershell
pytest tests\ -q          # 146개
```

한글이 깨지면 앞에 `$env:PYTHONIOENCODING="utf-8"` 을 붙이세요. 콘솔 문제일 뿐
데이터는 멀쩡합니다.

---

## 7. 지금 상태와 남은 일

### 되는 것
- 4개 탭 전부 렌더 · 테스트 151개 통과 · Cloud 배포 · 로그인 없이 접근 가능
- **데이터 7종(로더 5개) 전부 실데이터로 동작** — ARS + 판매 3종 + 골프 +
  가맹점 + **성별·연령**까지. 폴백으로 뜨는 것이 하나도 없습니다.
- 스크린샷 5장 (`docs/screenshots/`)
- 의존성 버전 `==` 고정 (심사 중 상위 버전이 나와도 앱이 안 바뀝니다)

### 아직 안 된 것
- 3분 시연 리허설
- **다크모드는 만들지 않기로 했습니다.** 라이트 단일 테마입니다
  (`.streamlit/config.toml` 의 `base = "light"`). 확인할 것이 없습니다.

### 뒤집힌 기록 하나 (2026-08-08)

이 문서는 나흘 동안 성별·연령 API 를 **"제공기관 서버가 죽었다"**고 적어 두었는데
**오진이었습니다.** `HTTP_ERROR(04)` 는 서버 장애와 **필수 파라미터 누락을 구분하지
않습니다** — `dateFrom`/`dateTo` 를 안 보내고 있었던 겁니다. 넣으니 812행이 들어왔고
지금은 실데이터로 뜹니다. "잘못된 키에는 403이 오니 인증은 통과했고 따라서 원본
서버 문제"라는 추론이 틀린 지점이었습니다 — 인증 통과는 원본 생존을 말해주지
않습니다. 자세한 경위는 README 의 해당 절에 있습니다.

받아보니 성격도 달랐습니다. **일자별**이지만 보유분이 2026-06-10~08-06 뿐이라
판매(2023~2024)와 **겹치는 날이 0일**이고, `CUST_WHOL_CNT` 는 방문객 수가 아니라
**누적 고객 수**입니다. 그래서 규모가 아니라 구성비로만 씁니다 — VRB 가 원래 비율만
차용하므로 발표 수치는 하나도 안 바뀝니다.

### 팀원이 맡아주면 좋은 것 (우선순위 순)

1. **발표 스토리 정리** — 3분. 731일 확보 후 권장 순서:
   **① r=0.80 이 731일에서도 그대로 → ② 요일 통제하니 0.62 (23%가 주말 효과) →
   ③ 세 채널 전부 p<0.001 로 견고, 1순위는 카지노 식음 → ④ 유입은 토요일 피크인데
   특산품만 일요일 피크 = 체크아웃 동선 → ⑤ 가맹점 큐레이션.**
   ②를 우리가 먼저 꺼내는 게 핵심입니다. 감추면 질문으로 돌아옵니다.

   ⚠ **④에서 "D+1" 이라고 말하지 마세요.** 731일로 재보니 특산품도 당일 상관이
   더 강합니다(D+0 0.313 > D+1 0.163). 남은 것은 **피크 요일 차이**뿐입니다.
   질문받으면 "31일치로는 D+1 이었는데 2년치로 다시 재서 철회했다"고 말하는 편이
   낫습니다 — 스스로 검증해 뒤집은 사례가 오히려 카드가 됩니다.

2. **2026년 판매 데이터 확보** — 지금 2026년은 유입만 있고 팔린 게 없어서 구간 C 가
   "유입 트렌드"만 보여줍니다. 이걸 채우면 최신 구간까지 전 체인이 살아납니다.

### 주의사항 몇 개

- **키를 코드나 파일에 커밋하지 마세요.** 로컬은 `.streamlit/secrets.toml`(gitignore),
  Cloud 는 앱 Settings → Secrets 에 넣습니다. 저장소가 Public 입니다.
- **새로 키를 넣을 일이 있으면 Decoding 쪽을 쓰세요.** 코드가 URL 인코딩을 자동으로
  해서, Encoding 키를 넣으면 이중 인코딩으로 인증에 실패합니다.
- **API 가 `04 HTTP_ERROR` 를 뱉으면 서버를 탓하기 전에 포털 문서의 필수 요청변수를
  대조하세요.** 04 는 서버 장애와 파라미터 누락을 구분하지 않습니다 — 우리가 이걸로
  나흘을 썼습니다.
- **스키마나 전처리를 바꿨으면 `data/processed/*.parquet` 을 지우세요.** 로더가 캐시를
  먼저 읽어서 옛 결과가 계속 나옵니다.
- **`tests/test_stats.py` 의 상수는 실측 회귀 기준값입니다.** 테스트가 깨졌다고 숫자를
  고치지 말고, 왜 바뀌었는지 먼저 확인하세요. 대부분 전처리 버그입니다.
- pandas 가 **3.0.5** 입니다(2.x 아님). 연쇄 대입 대신 `assign`/`loc` 을 쓰세요.
