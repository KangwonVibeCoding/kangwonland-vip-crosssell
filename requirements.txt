# Streamlit Community Cloud 는 루트의 이 파일만 본다 (packages.txt 불필요).
#
# 로컬에서 전 테스트(93개)가 통과한 조합을 `==` 로 고정한다. `>=` 로 두면 다음
# 배포가 언제 어떤 상위 버전을 끌어올지 알 수 없고, 심사 기간 중에 상위 버전이
# 나오면 그날 앱이 조용히 바뀐다. 발표 전날의 무증상 변경이 가장 위험하다.
#
# ⚠ `pip freeze` 전체를 붙여넣지 않는다. 개발 환경이 Windows 라 colorama·pywin32
# 같은 플랫폼 전용 패키지가 섞여 들어가고, 그러면 Linux 인 Cloud 에서 빌드가
# 깨진다. **직접 의존 7개만** 고정하고 전이 의존(altair·protobuf·tornado…)은
# pip 해석에 맡긴다 — streamlit 이 자기 호환 범위를 아는 쪽이 더 안전하다.
#
# 검증 환경: Python 3.13 (.python-version) · 2026-08-04
# 버전을 올릴 때는 pytest 전량 통과를 확인한 뒤 이 주석의 날짜를 갱신한다.
# 개발용(pytest)은 requirements-dev.txt 에 있다 — 배포 이미지에 넣지 않는다.

streamlit==1.60.0
pandas==3.0.5
numpy==2.5.1
plotly==6.9.0
pydeck==0.9.3
requests==2.34.2
pyarrow==24.0.0
